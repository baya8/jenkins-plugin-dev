version: '3'

services:

  java:
    build: .
    image: javadev
    container_name: javadev
    restart: always
    working_dir: /opt/app
    tty: true
    volumes:
      - ./app:/opt/app
      - ./.vscode-server:/root/.vscode-server
      - ./.m2:/root/.m2
    ports:
      - 2222:22